name: savvy_snackbars
description: "A simple, customizable Snackbar with optional loading indicators."
version: 0.0.3
homepage: https://github.com/kirtanpatel98/savvy_snackbars

environment:
  sdk: ">=3.4.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
  # assets:
  #   - assets/
